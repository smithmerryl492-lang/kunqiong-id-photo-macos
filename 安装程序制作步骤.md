# 鲲穹AI证件照 - 安装程序制作步骤

## 当前状态

✅ **安装程序正在编译中...**

编译命令已执行，正在压缩主程序文件（约245MB），预计需要5-10分钟完成。

## 制作步骤

### 1. 准备工作

确保以下文件/目录已准备好：
- ✅ `dist\鲲穹AI证件照\` - PyInstaller打包生成的目录
  - `鲲穹AI证件照.exe` - 主程序（245MB）
  - `_internal\` - 所有依赖文件和库
- ✅ `logo.ico` - 应用程序图标
- ✅ `ChineseSimplified.isl` - 简体中文语言文件
- ✅ `installer_setup.iss` - Inno Setup 配置文件

### 2. 编译方式

#### 方式一：使用批处理脚本（推荐）
```batch
编译安装程序.bat
```

#### 方式二：使用PowerShell命令
```powershell
cd e:\python_projet\pdf2word\idphoto_app_new_auth_code
& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer_setup.iss
```

#### 方式三：使用Inno Setup GUI
1. 打开 Inno Setup Compiler
2. 打开 `installer_setup.iss`
3. 点击菜单：Build → Compile

### 3. 编译过程

编译过程包括以下步骤：
1. ✅ 预处理和解析配置
2. ✅ 准备安装程序可执行文件
3. ✅ 解析各个区段配置
4. ⏳ **压缩文件（当前进行中）**
   - 主程序：245MB → 使用 lzma2/ultra64 压缩
   - _internal目录：所有依赖文件递归压缩
5. ⏸️ 创建最终安装程序
6. ⏸️ 签名和验证（如果配置）

**注意**：由于使用ultra64级别压缩，这个过程较慢但压缩比最高。

### 4. 预期输出

编译成功后，会在 `installer_output` 目录生成：

```
installer_output/
└── 鲲穹AI证件照_Setup_v1.0.0.exe
```

**安装程序大小**：预计 60-80MB（压缩后）

### 5. 安装程序功能

生成的安装程序包含以下功能：

#### 安装选项
- 默认安装位置：`C:\Program Files\鲲穹AI证件照`
- 可选桌面快捷方式
- 可选快速启动栏图标
- 开始菜单程序组

#### 安装内容
- 主程序：`鲲穹AI证件照.exe`
- 依赖文件：`_internal\` 目录下所有文件
- 卸载程序：自动生成

#### 智能功能
- 自动检测并卸载旧版本
- 中文安装界面
- 安装完成后可选启动程序
- 完整的卸载支持

### 6. 检查编译状态

#### 使用PowerShell检查
```powershell
# 检查输出文件是否生成
Get-ChildItem installer_output\鲲穹AI证件照_Setup_v1.0.0.exe -ErrorAction SilentlyContinue | 
    Format-List Name, @{Name="Size(MB)";Expression={[math]::Round($_.Length/1MB,2)}}, LastWriteTime
```

#### 查看进程
```powershell
# 检查Inno Setup进程
Get-Process ISCC -ErrorAction SilentlyContinue
```

### 7. 常见问题

#### Q: 编译速度很慢？
A: Ultra64压缩需要较长时间，特别是245MB的exe文件。这是正常的，请耐心等待。

#### Q: 出现"文件被占用"错误？
A: 
1. 关闭之前打开的安装程序
2. 删除 `installer_output` 目录下的旧文件
3. 终止可能运行的ISCC进程：`Get-Process ISCC | Stop-Process -Force`

#### Q: 如何加快编译速度？
A: 在 `installer_setup.iss` 中修改压缩设置：
```
Compression=lzma  ; 从 lzma2/ultra64 改为 lzma（快但压缩比低）
```

#### Q: 生成的安装程序在哪里？
A: `installer_output\鲲穹AI证件照_Setup_v1.0.0.exe`

### 8. 测试安装程序

编译完成后，建议进行以下测试：

1. **安装测试**
   - 双击运行安装程序
   - 检查安装界面是否正常
   - 完成安装并检查文件

2. **运行测试**
   - 从桌面快捷方式启动
   - 从开始菜单启动
   - 检查程序功能是否正常

3. **卸载测试**
   - 从控制面板卸载
   - 检查是否完全清理

### 9. 发布准备

安装程序测试通过后：

1. **重命名**（可选）
   ```
   鲲穹AI证件照_Setup_v1.0.0.exe
   → 
   鲲穹AI证件照安装程序.exe
   ```

2. **数字签名**（推荐）
   - 使用代码签名证书签名
   - 增加用户信任度
   - 避免Windows Defender警告

3. **发布**
   - 上传到官网/下载站
   - 记录MD5/SHA256哈希值
   - 提供下载链接

## 技术规格

### Inno Setup配置
- **压缩算法**：lzma2/ultra64
- **固实压缩**：是
- **最小Windows版本**：Windows 10
- **架构**：x64
- **权限要求**：最低权限（无需管理员）

### 文件结构
```
安装目录/
├── 鲲穹AI证件照.exe (245MB)
└── _internal/
    ├── Python运行时
    ├── PyQt6库
    ├── ONNX Runtime
    ├── OpenCV
    ├── models/ (AI模型)
    └── 其他依赖
```

### 安装程序特性
- 单文件安装程序
- 完整离线安装
- 无需Python环境
- 无需.NET Framework
- 绿色卸载

## 下一步

编译完成后，您可以：

1. 测试安装程序
2. 分发给用户
3. 收集反馈并优化

---

**编译时间估算**：5-10分钟（取决于CPU性能）
**生成文件大小**：约60-80MB
**支持的Windows版本**：Windows 10/11 (x64)
