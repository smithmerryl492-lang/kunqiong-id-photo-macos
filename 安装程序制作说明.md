# 鲲穹AI证件照 - 安装程序制作说明

## 前置要求

1. **下载并安装 Inno Setup**
   - 官网下载: https://jrsoftware.org/isdl.php
   - 推荐下载: **Inno Setup 6.x** (带有中文语言包)
   - 安装时选择完整安装,确保包含中文语言支持

## 制作安装程序步骤

### 方法一: 使用批处理文件(推荐)

1. 确保已安装 Inno Setup
2. 双击运行 `编译安装程序.bat`
3. 等待编译完成
4. 生成的安装程序位于 `installer_output` 文件夹中

### 方法二: 手动编译

1. 右键点击 `installer_setup.iss` 文件
2. 选择 "Compile" (编译)
3. 等待 Inno Setup 编译完成
4. 生成的安装程序位于 `installer_output` 文件夹中

### 方法三: 使用 Inno Setup GUI

1. 打开 Inno Setup Compiler
2. 点击 File -> Open
3. 选择 `installer_setup.iss` 文件
4. 点击 Build -> Compile (或按 F9)
5. 生成的安装程序位于 `installer_output` 文件夹中

## 配置说明

安装脚本文件 `installer_setup.iss` 中的主要配置项:

### 基本信息
```pascal
#define MyAppName "鲲穹AI证件照"         ; 应用程序名称
#define MyAppVersion "1.0.0"             ; 版本号
#define MyAppPublisher "鲲穹科技"         ; 发布者
#define MyAppURL "https://www.example.com" ; 官网地址
```

### 自定义设置

1. **修改应用图标**
   - 准备一个 `.ico` 格式的图标文件
   - 在脚本中取消注释并修改: `SetupIconFile=your_icon.ico`

2. **添加许可协议**
   - 准备 `LICENSE.txt` 文件
   - 在脚本中取消注释: `LicenseFile=LICENSE.txt`

3. **添加 README**
   - 准备 `README.txt` 文件
   - 在脚本中取消注释: `InfoBeforeFile=README.txt`

4. **修改默认安装位置**
   - 修改脚本中的: `DefaultDirName={autopf}\你的文件夹名`

## 生成的文件

编译成功后,在 `installer_output` 文件夹中会生成:
- `鲲穹AI证件照_Setup_v1.0.0.exe` - 安装程序

## 安装程序功能

用户运行安装程序后可以:
- ✅ 选择安装位置
- ✅ 创建桌面快捷方式(可选)
- ✅ 创建开始菜单项
- ✅ 创建快速启动栏图标(可选)
- ✅ 安装完成后自动运行程序(可选)
- ✅ 完整的卸载功能
- ✅ 检测并卸载旧版本

## 故障排除

### 问题1: 找不到 ISCC.exe
**解决方法**: 
- 检查 Inno Setup 是否正确安装
- 默认安装路径: `C:\Program Files (x86)\Inno Setup 6`
- 在批处理文件中修改 ISCC.exe 的路径

### 问题2: 编译失败
**解决方法**:
- 确保 `dist\鲲穹AI证件照` 文件夹存在
- 确保该文件夹中有 `鲲穹AI证件照.exe` 和 `_internal` 文件夹
- 检查文件路径中是否有特殊字符

### 问题3: 中文乱码
**解决方法**:
- 确保 Inno Setup 安装了中文语言包
- 脚本文件使用 UTF-8 编码保存

## 高级配置

### 添加运行时依赖检查
可以在脚本的 `[Code]` 段中添加环境检查代码,例如:
- 检查 .NET Framework 版本
- 检查 Visual C++ 运行库
- 检查 Windows 版本

### 静默安装支持
用户可以使用以下命令进行静默安装:
```
鲲穹AI证件照_Setup_v1.0.0.exe /SILENT /DIR="C:\自定义路径"
```

### 完全静默安装
```
鲲穹AI证件照_Setup_v1.0.0.exe /VERYSILENT /SUPPRESSMSGBOXES
```

## 版本更新

当需要发布新版本时:
1. 修改 `#define MyAppVersion "1.0.0"` 为新版本号
2. 重新编译安装程序
3. 新版本安装时会自动检测并提示卸载旧版本

## 技术支持

如有问题,请检查:
- Inno Setup 官方文档: https://jrsoftware.org/ishelp/
- 本地帮助文件: Inno Setup 安装目录中的 `ISetup.chm`
